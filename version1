<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <meta name="theme-color" content="#f5f0e8" />
  <title>Study Log</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <style>
    :root, [data-theme="light"] {
      --font-display: 'Playfair Display', Georgia, serif;
      --font-body: 'Lato', 'Helvetica Neue', sans-serif;
      --text-xs: clamp(0.75rem, 0.7rem + 0.25vw, 0.875rem);
      --text-sm: clamp(0.875rem, 0.8rem + 0.35vw, 1rem);
      --text-base: clamp(1rem, 0.95rem + 0.25vw, 1.125rem);
      --text-lg: clamp(1.125rem, 1rem + 0.75vw, 1.5rem);
      --text-xl: clamp(1.5rem, 1.2rem + 1.25vw, 2.1rem);
      --space-1: .25rem; --space-2: .5rem; --space-3: .75rem; --space-4: 1rem; --space-5: 1.25rem; --space-6: 1.5rem; --space-8: 2rem; --space-10: 2.5rem; --space-12: 3rem;
      --color-bg: #f5f0e8;
      --color-surface: #f9f6ef;
      --color-surface-2: #fdfbf7;
      --color-surface-offset: #ede6d8;
      --color-divider: #d9d0c0;
      --color-border: #ccc3b0;
      --color-text: #2c241a;
      --color-text-muted: #6b5d4e;
      --color-text-faint: #a89880;
      --color-text-inverse: #f5f0e8;
      --color-primary: #3d6b4f;
      --color-primary-hover: #2e5239;
      --color-primary-highlight: #d4e3da;
      --color-accent: #8b3a3a;
      --color-accent-highlight: #e8d5d5;
      --radius-sm: .25rem; --radius-md: .5rem; --radius-lg: .75rem; --radius-xl: 1rem; --radius-full: 9999px;
      --shadow-sm: 0 1px 3px oklch(0.2 0.02 60 / 0.08);
      --shadow-md: 0 4px 12px oklch(0.2 0.02 60 / 0.10);
      --transition: 180ms cubic-bezier(0.16,1,0.3,1);
      --safe-top: env(safe-area-inset-top, 0px);
      --safe-bottom: env(safe-area-inset-bottom, 0px);
    }
    [data-theme="dark"] {
      --color-bg: #191612;
      --color-surface: #211d18;
      --color-surface-2: #28231d;
      --color-surface-offset: #2f281f;
      --color-divider: #423a31;
      --color-border: #54483b;
      --color-text: #e8dfd1;
      --color-text-muted: #b7a998;
      --color-text-faint: #8d7f70;
      --color-text-inverse: #1b1712;
      --color-primary: #79a88c;
      --color-primary-hover: #67937a;
      --color-primary-highlight: #2e3d33;
      --color-accent: #ca8d8d;
      --color-accent-highlight: #423030;
    }
    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{-webkit-font-smoothing:antialiased;text-rendering:optimizeLegibility}
    body{min-height:100dvh;font-family:var(--font-body);font-size:var(--text-base);line-height:1.6;color:var(--color-text);background:var(--color-bg);max-width:480px;margin:0 auto;padding-top:var(--safe-top)}
    button,input,select,textarea{font:inherit;color:inherit} button{border:none;background:none;cursor:pointer}
    h1,h2,h3{font-family:var(--font-display);line-height:1.15} p{max-width:65ch}
    a,button,input,select,textarea{transition:background var(--transition),color var(--transition),border-color var(--transition),box-shadow var(--transition),transform var(--transition)}
    :focus-visible{outline:2px solid var(--color-primary);outline-offset:3px;border-radius:var(--radius-sm)}
    .app-header{position:sticky;top:0;z-index:10;background:var(--color-bg);border-bottom:1px solid var(--color-divider);padding:var(--space-4) var(--space-5);display:flex;align-items:center;justify-content:space-between}
    .app-logo{display:flex;align-items:center;gap:var(--space-2);font-family:var(--font-display);font-size:var(--text-lg);font-weight:700}
    .app-logo svg{color:var(--color-primary)}
    .header-right{display:flex;align-items:center;gap:var(--space-3)}
    .date-label{font-size:var(--text-xs);color:var(--color-text-muted);font-style:italic}
    .theme-toggle{width:40px;height:40px;border-radius:50%;border:1px solid var(--color-border);display:grid;place-items:center;background:var(--color-surface)}
    .page-content{padding:var(--space-5) var(--space-5) calc(92px + var(--safe-bottom));min-height:calc(100dvh - 60px)}
    .page{display:none}.page.active{display:block}
    .page-title{font-size:var(--text-xl);margin-bottom:var(--space-1)}
    .page-subtitle{font-size:var(--text-sm);color:var(--color-text-muted);font-style:italic;margin-bottom:var(--space-5)}
    .section-label{font-size:var(--text-xs);letter-spacing:.1em;text-transform:uppercase;color:var(--color-text-faint);font-weight:700;margin:var(--space-5) 0 var(--space-3)}
    .card{background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);box-shadow:var(--shadow-sm)}
    .hero-card{padding:var(--space-5);margin-bottom:var(--space-5);background:linear-gradient(180deg,var(--color-surface),var(--color-surface-2))}
    .hero-meta{display:flex;justify-content:space-between;gap:var(--space-3);font-size:var(--text-xs);color:var(--color-text-muted);text-transform:uppercase;letter-spacing:.08em}
    .hero-task{font-family:var(--font-display);font-size:clamp(2rem,1.7rem + 2vw,2.8rem);margin:var(--space-4) 0 var(--space-2)}
    .hero-subject{font-size:var(--text-sm);color:var(--color-text-muted);font-style:italic}
    .timer-wrap{margin:var(--space-5) 0;display:grid;place-items:center}
    .timer-ring{width:210px;height:210px;border-radius:50%;background:radial-gradient(circle at 50% 50%,var(--color-surface-2) 58%,transparent 59%),conic-gradient(var(--color-primary) calc(var(--progress,0)*1%),var(--color-surface-offset) 0);display:grid;place-items:center;box-shadow:inset 0 0 0 1px var(--color-border),var(--shadow-sm)}
    .timer-inner{width:156px;height:156px;border-radius:50%;background:var(--color-bg);display:grid;place-items:center;text-align:center;padding:var(--space-3)}
    .timer-time{font-family:var(--font-display);font-size:2.15rem;font-variant-numeric:tabular-nums}
    .timer-note{font-size:var(--text-xs);color:var(--color-text-muted);text-transform:uppercase;letter-spacing:.08em}
    .slide-shell{background:var(--color-surface-offset);border:1px solid var(--color-border);border-radius:var(--radius-full);padding:6px;position:relative;overflow:hidden;margin-top:var(--space-4)}
    .slide-track{height:58px;position:relative;user-select:none}
    .slide-label{position:absolute;inset:0;display:grid;place-items:center;font-size:var(--text-sm);font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--color-text-muted)}
    .slide-thumb{position:absolute;left:0;top:0;width:58px;height:58px;border-radius:50%;background:var(--color-primary);display:grid;place-items:center;color:white;box-shadow:var(--shadow-md);touch-action:none}
    .slide-thumb svg{width:22px;height:22px;stroke:currentColor;fill:none;stroke-width:2}
    .btn-row{display:flex;gap:var(--space-3);margin-top:var(--space-4)}
    .btn{flex:1;display:inline-flex;align-items:center;justify-content:center;gap:var(--space-2);padding:var(--space-3) var(--space-4);border-radius:var(--radius-md);font-size:13px;font-weight:700;letter-spacing:.06em;text-transform:uppercase}
    .btn-primary{background:var(--color-primary);color:white}.btn-secondary{border:1px solid var(--color-border);background:transparent}.btn-danger{border:1px solid var(--color-accent);color:var(--color-accent);background:transparent}
    .mini-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:var(--space-3);margin-bottom:var(--space-5)}
    .stat-card{padding:var(--space-4)} .stat-label{font-size:var(--text-xs);letter-spacing:.08em;text-transform:uppercase;color:var(--color-text-faint);font-weight:700} .stat-value{font-family:var(--font-display);font-size:var(--text-lg);margin-top:2px}
    .task-list,.history-list,.project-list{display:flex;flex-direction:column;gap:var(--space-3)}
    .task-card,.history-card,.project-card{padding:var(--space-4) var(--space-5)}
    .task-top,.project-top,.history-top{display:flex;justify-content:space-between;gap:var(--space-3);align-items:start}
    .task-name,.project-name,.history-name{font-size:var(--text-base);color:var(--color-text)}
    .meta-row{display:flex;flex-wrap:wrap;gap:var(--space-3);margin-top:4px;font-size:var(--text-xs);color:var(--color-text-faint);font-style:italic}
    .badge{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;padding:2px 8px;border-radius:var(--radius-full)}
    .badge.primary{background:var(--color-primary-highlight);color:var(--color-primary)} .badge.accent{background:var(--color-accent-highlight);color:var(--color-accent)}
    .project-card.active-project{border-color:var(--color-primary)}
    .history-actions,.project-actions{display:flex;gap:var(--space-2);margin-top:var(--space-4)}
    .ghost-btn{padding:var(--space-2) var(--space-3);border-radius:var(--radius-md);border:1px solid var(--color-border);font-size:11px;text-transform:uppercase;letter-spacing:.06em;color:var(--color-text-muted)}
    .empty-state{padding:var(--space-10) var(--space-5);text-align:center;color:var(--color-text-muted);background:var(--color-surface);border:1px dashed var(--color-border);border-radius:var(--radius-lg)}
    .empty-state h3{font-size:var(--text-lg);margin-bottom:var(--space-2);color:var(--color-text)}
    .form-card{padding:var(--space-5)}
    .form-group{margin-bottom:var(--space-4)}
    .form-label{display:block;font-size:var(--text-sm);font-weight:700;margin-bottom:var(--space-2)}
    .form-input,.form-select,.form-textarea{width:100%;padding:var(--space-3) var(--space-4);border:1.5px solid var(--color-border);border-radius:var(--radius-md);background:var(--color-bg)}
    .form-textarea{min-height:92px;resize:vertical}
    .bottom-nav{position:fixed;bottom:0;left:50%;transform:translateX(-50%);width:100%;max-width:480px;background:var(--color-surface);border-top:1px solid var(--color-divider);display:flex;z-index:12;padding-bottom:var(--safe-bottom);box-shadow:0 -2px 12px oklch(0.2 0.02 60 / 0.06)}
    .nav-btn{flex:1;padding:var(--space-3) var(--space-2);display:flex;flex-direction:column;align-items:center;gap:3px;color:var(--color-text-faint);font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.05em;border-top:2px solid transparent}
    .nav-btn.active{color:var(--color-primary);border-top-color:var(--color-primary)}
    .nav-btn svg{width:22px;height:22px;stroke:currentColor;fill:none;stroke-width:1.8}
    .toast{position:fixed;left:50%;bottom:calc(86px + var(--safe-bottom));transform:translateX(-50%);background:var(--color-text);color:var(--color-text-inverse);padding:var(--space-3) var(--space-5);border-radius:var(--radius-full);font-size:var(--text-sm);box-shadow:var(--shadow-md);z-index:25;opacity:0;pointer-events:none}
    .toast.show{opacity:1}
    canvas{margin-top:var(--space-4);background:var(--color-surface);border:1px solid var(--color-border);border-radius:var(--radius-lg);padding:var(--space-4)}
    @media (prefers-reduced-motion: reduce){*,*::before,*::after{animation-duration:.01ms!important;transition-duration:.01ms!important}}
  </style>
</head>
<body>
  <header class="app-header">
    <div class="app-logo">
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/><path d="M9 7h6M9 11h4"/></svg>
      Study Log
    </div>
    <div class="header-right">
      <span class="date-label" id="header-date"></span>
      <button class="theme-toggle" data-theme-toggle aria-label="Toggle theme"></button>
    </div>
  </header>
  <main>
    <section class="page active" id="page-today">
      <div class="page-content">
        <h1 class="page-title">Today</h1>
        <p class="page-subtitle">Ready to get some work done?</p>
        <div class="card hero-card">
          <div class="hero-meta"><span id="heroProjectStatus">No active project</span><span id="heroSessionCount">0 sessions this week</span></div>
          <div class="hero-task" id="heroTask">Start a study session</div>
          <div class="hero-subject" id="heroSubject">Choose a project below or create one in Projects.</div>
          <div class="timer-wrap">
            <div class="timer-ring" id="timerRing" style="--progress:0">
              <div class="timer-inner">
                <div class="timer-time" id="timerDisplay">00:00:00</div>
                <div class="timer-note" id="timerState">Ready to focus</div>
              </div>
            </div>
          </div>
          <div class="slide-shell" id="sliderShell">
            <div class="slide-track" id="slideTrack">
              <div class="slide-label" id="slideLabel">Slide to finish</div>
              <div class="slide-thumb" id="slideThumb" aria-label="Slide to finish session" role="button" tabindex="0">
                <svg viewBox="0 0 24 24"><path d="M5 12h14"/><path d="M13 6l6 6-6 6"/></svg>
              </div>
            </div>
          </div>
          <div class="btn-row">
            <button class="btn btn-primary" id="startPauseBtn">Start</button>
            <button class="btn btn-secondary" id="manualAddBtn">Manual log</button>
          </div>
          <div class="btn-row">
            <button class="btn btn-secondary" id="exportCsvBtn">Export CSV</button>
            <button class="btn btn-secondary" id="exportJsonBtn">Backup JSON</button>
          </div>
        </div>
        <div class="mini-grid">
          <div class="card stat-card"><div class="stat-label">Today</div><div class="stat-value" id="todayMinutes">0 min</div></div>
          <div class="card stat-card"><div class="stat-label">Week</div><div class="stat-value" id="weekMinutes">0 min</div></div>
        </div>
        <div class="section-label">Quick projects</div>
        <div class="project-list" id="todayProjectList"></div>
      </div>
    </section>
    <section class="page" id="page-history">
      <div class="page-content">
        <h1 class="page-title">History</h1>
        <p class="page-subtitle">Your study logs, editable and export-ready.</p>
        <div class="history-list" id="historyList"></div>
      </div>
    </section>
    <section class="page" id="page-stats">
      <div class="page-content">
        <h1 class="page-title">Stats</h1>
        <p class="page-subtitle">Weekly time by project.</p>
        <div class="mini-grid">
          <div class="card stat-card"><div class="stat-label">Sessions</div><div class="stat-value" id="statsSessions">0</div></div>
          <div class="card stat-card"><div class="stat-label">Projects</div><div class="stat-value" id="statsProjects">0</div></div>
        </div>
        <canvas id="statsChart" height="240"></canvas>
      </div>
    </section>
    <section class="page" id="page-projects">
      <div class="page-content">
        <h1 class="page-title">Projects</h1>
        <p class="page-subtitle">Make each assignment or class feel like a kept list in a quiet planner.</p>
        <div class="card form-card">
          <div class="form-group"><label class="form-label" for="projectName">Project name</label><input class="form-input" id="projectName" placeholder="History essay" /></div>
          <div class="form-group"><label class="form-label" for="projectSubject">Subject</label><input class="form-input" id="projectSubject" placeholder="History" /></div>
          <div class="form-group"><label class="form-label" for="projectGoal">Weekly goal minutes</label><input class="form-input" id="projectGoal" type="number" min="0" value="120" /></div>
          <button class="btn btn-primary" style="width:100%" id="addProjectBtn">Add project</button>
        </div>
        <div class="section-label">Your projects</div>
        <div class="project-list" id="projectList"></div>
      </div>
    </section>
  </main>
  <nav class="bottom-nav" aria-label="Primary">
    <button class="nav-btn active" data-target="today"><svg viewBox="0 0 24 24"><path d="M3 11.5 12 4l9 7.5"/><path d="M5.5 10.5V20h13V10.5"/></svg><span>Today</span></button>
    <button class="nav-btn" data-target="history"><svg viewBox="0 0 24 24"><path d="M12 8v5l3 2"/><circle cx="12" cy="12" r="9"/></svg><span>History</span></button>
    <button class="nav-btn" data-target="stats"><svg viewBox="0 0 24 24"><path d="M4 19h16"/><path d="M7 16V9"/><path d="M12 16V5"/><path d="M17 16v-7"/></svg><span>Stats</span></button>
    <button class="nav-btn" data-target="projects"><svg viewBox="0 0 24 24"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/><path d="M9 7h6M9 11h4"/></svg><span>Projects</span></button>
  </nav>
  <div class="toast" id="toast"></div>
  <script>
    const state = {
      theme: matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light',
      projects: [
        {id:'p1', name:'Bible quiz review', subject:'Bible', goal:150},
        {id:'p2', name:'Machining homework', subject:'CNC', goal:120},
        {id:'p3', name:'History reading', subject:'History', goal:90}
      ],
      logs: [
        {id:'l1', projectId:'p1', project:'Bible quiz review', subject:'Bible', start:'2026-04-28T18:00:00', end:'2026-04-28T18:42:00', durationMin:42, notes:'Romans memory review'},
        {id:'l2', projectId:'p2', project:'Machining homework', subject:'CNC', start:'2026-04-29T15:10:00', end:'2026-04-29T16:05:00', durationMin:55, notes:'Tolerance worksheet'},
        {id:'l3', projectId:'p1', project:'Bible quiz review', subject:'Bible', start:'2026-04-30T07:15:00', end:'2026-04-30T08:00:00', durationMin:45, notes:'Practice questions'}
      ],
      activeProjectId:'p1',
      timerRunning:false,
      timerStartedAt:null,
      elapsedBeforePause:0,
      chart:null
    };

    const $ = s => document.querySelector(s);
    const $$ = s => [...document.querySelectorAll(s)];
    const fmtDate = d => new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric'}).format(d);
    const fmtDateLong = d => new Intl.DateTimeFormat(undefined,{weekday:'short',month:'short',day:'numeric'}).format(d);
    const fmtTime = d => new Intl.DateTimeFormat(undefined,{hour:'numeric',minute:'2-digit'}).format(d);
    const fmtDuration = min => min >= 60 ? `${Math.floor(min/60)}h ${min%60}m` : `${min} min`;
    const now = () => new Date();

    document.documentElement.setAttribute('data-theme', state.theme);
    function setThemeButton(){
      const t = $('[data-theme-toggle]');
      t.innerHTML = state.theme === 'dark'
        ? '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="5"/><path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>'
        : '<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>';
    }
    setThemeButton();
    $('[data-theme-toggle]').addEventListener('click',()=>{state.theme=state.theme==='dark'?'light':'dark';document.documentElement.setAttribute('data-theme',state.theme);setThemeButton();renderStats();});

    function getActiveProject(){ return state.projects.find(p => p.id === state.activeProjectId) || null; }
    function getElapsedMs(){ return state.timerRunning ? state.elapsedBeforePause + (Date.now() - state.timerStartedAt) : state.elapsedBeforePause; }
    function formatClock(ms){ const s=Math.floor(ms/1000); const h=String(Math.floor(s/3600)).padStart(2,'0'); const m=String(Math.floor((s%3600)/60)).padStart(2,'0'); const sec=String(s%60).padStart(2,'0'); return `${h}:${m}:${sec}`; }
    function startTimer(){ if(!getActiveProject()) return toast('Create or select a project first'); state.timerRunning=true; state.timerStartedAt=Date.now(); $('#startPauseBtn').textContent='Pause'; $('#timerState').textContent='Tracking now'; render(); }
    function pauseTimer(){ state.timerRunning=false; state.elapsedBeforePause=getElapsedMs(); state.timerStartedAt=null; $('#startPauseBtn').textContent='Resume'; $('#timerState').textContent='Paused'; render(); }
    function resetSlider(){ const thumb=$('#slideThumb'); thumb.style.left='0px'; $('#slideLabel').textContent='Slide to finish'; }
    function finishSession(){ const ms=getElapsedMs(); if(ms < 60000) return toast('Track at least 1 minute first'); const p=getActiveProject(); const end=new Date(); const start=new Date(end.getTime()-ms); const min=Math.max(1,Math.round(ms/60000)); state.logs.unshift({id:'l'+Math.random().toString(36).slice(2,9), projectId:p.id, project:p.name, subject:p.subject, start:start.toISOString(), end:end.toISOString(), durationMin:min, notes:'Timed session'}); state.timerRunning=false; state.timerStartedAt=null; state.elapsedBeforePause=0; $('#startPauseBtn').textContent='Start'; $('#timerState').textContent='Ready to focus'; resetSlider(); render(); toast('Session saved'); }
    function addManualLog(){ const p=getActiveProject(); if(!p) return toast('Select a project first'); const min = Number(prompt('Minutes studied?', '45')); if(!min || min < 1) return; const note = prompt('Notes (optional)', '') || ''; const end = new Date(); const start = new Date(end.getTime() - min*60000); state.logs.unshift({id:'l'+Math.random().toString(36).slice(2,9), projectId:p.id, project:p.name, subject:p.subject, start:start.toISOString(), end:end.toISOString(), durationMin:min, notes:note}); render(); toast('Manual log added'); }
    function exportCSV(){
      const rows = [['Date','Project','Subject','Start','End','Duration Minutes','Notes'], ...state.logs.map(l => [l.start.slice(0,10),l.project,l.subject,new Date(l.start).toLocaleString(),new Date(l.end).toLocaleString(),l.durationMin,l.notes])];
      const csv = rows.map(r => r.map(v => `"${String(v??'').replaceAll('"','""')}"`).join(',')).join('\n');
      downloadBlob(csv, 'study-logs.csv', 'text/csv;charset=utf-8;');
      toast('CSV exported');
    }
    function exportJSON(){ downloadBlob(JSON.stringify({projects:state.projects,logs:state.logs},null,2), 'study-log-backup.json', 'application/json'); toast('Backup exported'); }
    function downloadBlob(content, name, type){ const blob = new Blob([content], {type}); const a = document.createElement('a'); a.href = URL.createObjectURL(blob); a.download = name; a.click(); setTimeout(()=>URL.revokeObjectURL(a.href), 1000); }
    function totalToday(){ const today = new Date().toISOString().slice(0,10); return state.logs.filter(l => l.start.slice(0,10)===today).reduce((a,b)=>a+b.durationMin,0); }
    function totalWeek(){ const d = now(); const start = new Date(d); start.setDate(d.getDate()-6); start.setHours(0,0,0,0); return state.logs.filter(l => new Date(l.start) >= start).reduce((a,b)=>a+b.durationMin,0); }
    function weekSessions(){ const d = now(); const start = new Date(d); start.setDate(d.getDate()-6); start.setHours(0,0,0,0); return state.logs.filter(l => new Date(l.start) >= start); }

    function renderProjects(listId, compact=false){
      const box=$(listId); box.innerHTML='';
      if(!state.projects.length){ box.innerHTML='<div class="empty-state"><h3>No projects yet</h3><p>Add a class, assignment, or study thread to start tracking against it.</p></div>'; return; }
      state.projects.forEach(p=>{
        const mins = state.logs.filter(l=>l.projectId===p.id).reduce((a,b)=>a+b.durationMin,0);
        const el=document.createElement('div');
        el.className=`card project-card ${state.activeProjectId===p.id?'active-project':''}`;
        el.innerHTML=`<div class="project-top"><div><div class="project-name">${p.name}</div><div class="meta-row"><span>${p.subject}</span><span>${fmtDuration(mins)} logged</span></div></div><span class="badge primary">Goal ${p.goal}m</span></div>${compact?'':`<div class="project-actions"><button class="ghost-btn" data-select="${p.id}">Make active</button><button class="ghost-btn" data-delete="${p.id}">Delete</button></div>`}`;
        if(compact){ el.addEventListener('click',()=>{state.activeProjectId=p.id; render(); toast(`${p.name} selected`);}); }
        box.appendChild(el);
      });
      box.querySelectorAll('[data-select]').forEach(b=>b.onclick=()=>{state.activeProjectId=b.dataset.select; render(); toast('Active project updated');});
      box.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>{ state.projects=state.projects.filter(p=>p.id!==b.dataset.delete); if(state.activeProjectId===b.dataset.delete) state.activeProjectId=state.projects[0]?.id||null; state.logs=state.logs.filter(l=>l.projectId!==b.dataset.delete); render(); toast('Project deleted'); });
    }

    function renderHistory(){
      const box=$('#historyList'); box.innerHTML='';
      if(!state.logs.length){ box.innerHTML='<div class="empty-state"><h3>No study logs yet</h3><p>Your finished timer sessions and manual entries will appear here.</p></div>'; return; }
      state.logs.forEach(l=>{
        const s=new Date(l.start), e=new Date(l.end);
        const el=document.createElement('div');
        el.className='card history-card';
        el.innerHTML=`<div class="history-top"><div><div class="history-name">${l.project}</div><div class="meta-row"><span>${l.subject}</span><span>${fmtDateLong(s)}</span><span>${fmtTime(s)}-${fmtTime(e)}</span></div></div><span class="badge accent">${fmtDuration(l.durationMin)}</span></div>${l.notes?`<div class="meta-row" style="margin-top:10px;font-style:normal;color:var(--color-text-muted)">${l.notes}</div>`:''}<div class="history-actions"><button class="ghost-btn" data-edit="${l.id}">Edit minutes</button><button class="ghost-btn" data-remove="${l.id}">Delete</button></div>`;
        box.appendChild(el);
      });
      box.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const log=state.logs.find(l=>l.id===b.dataset.edit); const val=Number(prompt('New minutes', log.durationMin)); if(!val||val<1) return; log.durationMin=val; const end=new Date(log.end); log.start=new Date(end.getTime()-val*60000).toISOString(); render(); toast('Log updated');});
      box.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{state.logs=state.logs.filter(l=>l.id!==b.dataset.remove); render(); toast('Log removed');});
    }

    function renderStats(){
      const weekly = {};
      weekSessions().forEach(l=> weekly[l.project] = (weekly[l.project]||0) + l.durationMin);
      $('#statsSessions').textContent = state.logs.length;
      $('#statsProjects').textContent = state.projects.length;
      const labels = Object.keys(weekly); const vals = Object.values(weekly);
      const ctx = $('#statsChart');
      if(state.chart) state.chart.destroy();
      state.chart = new Chart(ctx, {type:'bar', data:{labels, datasets:[{label:'Minutes this week', data:vals, backgroundColor:getComputedStyle(document.documentElement).getPropertyValue('--color-primary').trim(), borderRadius:8}]}, options:{plugins:{legend:{display:false}}, scales:{y:{beginAtZero:true, ticks:{color:getComputedStyle(document.documentElement).getPropertyValue('--color-text-muted').trim()}, grid:{color:getComputedStyle(document.documentElement).getPropertyValue('--color-divider').trim()}}, x:{ticks:{color:getComputedStyle(document.documentElement).getPropertyValue('--color-text-muted').trim()}, grid:{display:false}}}}});
    }

    function renderHero(){
      const p=getActiveProject();
      $('#heroProjectStatus').textContent = p ? `Active project · ${p.subject}` : 'No active project';
      $('#heroSessionCount').textContent = `${weekSessions().length} sessions this week`;
      $('#heroTask').textContent = p ? p.name : 'Start a study session';
      $('#heroSubject').textContent = p ? `Weekly goal ${p.goal} minutes` : 'Choose a project below or create one in Projects.';
      $('#todayMinutes').textContent = fmtDuration(totalToday());
      $('#weekMinutes').textContent = fmtDuration(totalWeek());
      $('#timerDisplay').textContent = formatClock(getElapsedMs());
      $('#timerRing').style.setProperty('--progress', Math.min(100, (getElapsedMs() / (60*60*1000))*100));
      if(!state.timerRunning && state.elapsedBeforePause===0) $('#timerState').textContent = 'Ready to focus';
    }

    function render(){
      $('#header-date').textContent = fmtDate(now());
      renderHero();
      renderProjects('#todayProjectList', true);
      renderProjects('#projectList', false);
      renderHistory();
      renderStats();
    }

    function toast(msg){ const t=$('#toast'); t.textContent=msg; t.classList.add('show'); clearTimeout(window.toastTimer); window.toastTimer=setTimeout(()=>t.classList.remove('show'), 1800); }

    $('#startPauseBtn').addEventListener('click',()=>{ if(state.timerRunning) pauseTimer(); else startTimer(); });
    $('#manualAddBtn').addEventListener('click', addManualLog);
    $('#exportCsvBtn').addEventListener('click', exportCSV);
    $('#exportJsonBtn').addEventListener('click', exportJSON);
    $('#addProjectBtn').addEventListener('click',()=>{
      const name=$('#projectName').value.trim(), subject=$('#projectSubject').value.trim(), goal=Number($('#projectGoal').value)||0;
      if(!name || !subject) return toast('Add both project and subject');
      const p={id:'p'+Math.random().toString(36).slice(2,8), name, subject, goal};
      state.projects.unshift(p); state.activeProjectId=p.id; $('#projectName').value=''; $('#projectSubject').value=''; $('#projectGoal').value='120'; render(); toast('Project added');
    });

    $$('.nav-btn').forEach(btn=>btn.addEventListener('click',()=>{
      $$('.nav-btn').forEach(b=>b.classList.remove('active')); btn.classList.add('active');
      $$('.page').forEach(p=>p.classList.remove('active')); $('#page-'+btn.dataset.target).classList.add('active');
    }));

    const slideTrack = $('#slideTrack'); const slideThumb = $('#slideThumb'); let dragging=false, startX=0, startLeft=0;
    const maxLeft = () => slideTrack.clientWidth - slideThumb.clientWidth;
    function setLeft(x){ const left=Math.max(0, Math.min(maxLeft(), x)); slideThumb.style.left=left+'px'; const ratio=left/maxLeft(); $('#slideLabel').textContent = ratio > .72 ? 'Release to save' : 'Slide to finish'; }
    function pointerDown(clientX){ if(!state.timerRunning && state.elapsedBeforePause===0){ toast('Start a session first'); return; } dragging=true; startX=clientX; startLeft=parseFloat(slideThumb.style.left||'0')||0; slideThumb.style.transition='none'; }
    function pointerMove(clientX){ if(!dragging) return; setLeft(startLeft + clientX - startX); }
    function pointerUp(){ if(!dragging) return; dragging=false; const left=parseFloat(slideThumb.style.left||'0')||0; slideThumb.style.transition='left 180ms cubic-bezier(0.16,1,0.3,1)'; if(left >= maxLeft()*0.86){ slideThumb.style.left=maxLeft()+'px'; setTimeout(finishSession, 120); } else resetSlider(); }
    slideThumb.addEventListener('pointerdown',e=>{ slideThumb.setPointerCapture(e.pointerId); pointerDown(e.clientX); });
    slideThumb.addEventListener('pointermove',e=>pointerMove(e.clientX));
    slideThumb.addEventListener('pointerup',pointerUp); slideThumb.addEventListener('pointercancel',pointerUp);
    slideThumb.addEventListener('keydown',e=>{ if(e.key==='Enter' || e.key===' '){ e.preventDefault(); finishSession(); }});

    setInterval(()=>{ if(state.timerRunning) renderHero(); }, 1000);
    render();
  </script>
</body>
</html>
